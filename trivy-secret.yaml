# rules:
#   - id: aws-access-key-id
#   - id: aws-secret-access-key  
#   - id: stripe-secret-token
#   - id: github-pat
#   - id: generic-api-key

# # Note: This may not fully override allowlist behavior,
# # but it ensures these rule types are enabled
